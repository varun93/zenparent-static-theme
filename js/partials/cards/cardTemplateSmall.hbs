<div class="col-lg-12 card card-small col-md-12 col-sm-12">
  <a href="{{permalink}}" >
      <img class="img-responsive" src="{{image}}">
      <div class="overlay">
        {{title}}
      </div>
  </a>
  {{#if category}}
      <a href="#" class="category-label {{category}}">
        <div>
          <span>
            {{category}}
          </span>
        </div>
      </a>
  {{/if}}
</div>
